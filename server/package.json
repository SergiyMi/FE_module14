{
  "name": "server",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "json-server --delay 200 --port 3000 --watch db.json"
  },
  "dependencies": {
    "json-server": "^0.15.1"
  }
}
